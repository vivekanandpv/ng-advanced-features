import { TextWeightDirective } from './text-weight.directive';

describe('TextWeightDirective', () => {
  it('should create an instance', () => {
    const directive = new TextWeightDirective();
    expect(directive).toBeTruthy();
  });
});
