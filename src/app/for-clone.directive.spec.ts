import { ForCloneDirective } from './for-clone.directive';

describe('ForCloneDirective', () => {
  it('should create an instance', () => {
    const directive = new ForCloneDirective();
    expect(directive).toBeTruthy();
  });
});
